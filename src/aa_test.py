import time

# from constants import p_al, p_xe
import os
from dotenv import load_dotenv
from utilities import timediff

# load_dotenv()  # take environment variables from .env file

# start_time = time.time()
# print(os.getenv("PORTAL_UN"), os.getenv("PORTAL_PW"), p_al, p_xe)
# end_time = time.time()
# print(f"Elapsed time: {timediff(start_time, end_time)}")

print(os.environ)
